---
name: harvest-plan-defect
tags: [harvest-findings, contract]
runs: 1
max_turns: 15
---
Run /harvest-findings 1 against the sampleapp fixture (docs/epics/epic-1-test-plan.md). `gh` is unavailable — skip tracking and say so. Do everything else the skill specifies: capture to docs/epics.md first, route by kind, author the remediation story via create-story if warranted, reset the plan.
