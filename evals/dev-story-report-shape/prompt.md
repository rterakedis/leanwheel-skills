---
name: dev-story-report-shape
tags: [dev-story, contract]
runs: 1
max_turns: 12
---
You are finishing story 1.1 in the sampleapp fixture (docs/epics/1-1-customer-limit-service.md). Assume implementation, build, and tests are already green and `CustomerLimitServiceTests` plus the `UpgradeSheetFlow` UI test exist. Do NOT run any toolchain. Produce only the dev-story completion report as the `lw-story-developer` contract defines it (see the dev-story skill → "Testing Plan (required report field)"), including the TESTING PLAN block.
