---
name: architecture
description: Create or update the technical architecture document. Use when the user says "create architecture" or "update architecture".
---

# Architecture (alias)

This skill's flows moved to `/spec` with target `architecture` (`skills/spec/SKILL.md`),
which renders `docs/architecture.md` from the decision log and
`spec/architecture-template.md`.

Invoke `/spec` with target `architecture` now and follow it. Do not follow any remembered
version of this skill's old section-by-section flow.
