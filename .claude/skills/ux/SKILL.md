---
name: ux
description: Plan UX design and produce DESIGN.md + EXPERIENCE.md. Use when the user wants to create, update, or validate UX/design specs.
---

# UX (alias)

This skill's flows moved to `/spec` with target `ux` (`skills/spec/SKILL.md`), which renders
`docs/ux/DESIGN.md` + `docs/ux/EXPERIENCE.md` from the decision log, the templates in
`spec/`, and the platform presets in `spec/ux-presets.md`.

Invoke `/spec` with target `ux` now and follow it. Do not follow any remembered version of
this skill's old discovery flow.
