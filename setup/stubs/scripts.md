# Scripts — {project_name}

Runnable scripts and what they do. All paths relative to the project root.

## Development

| Script | Command | What it does | Expected output |
|--------|---------|-------------|-----------------|
| Dev server | `{command}` | {description} | {what you see} |
| Tests | `{command}` | {description} | {what you see} |
| Lint | `{command}` | {description} | {what you see} |

## Database

| Script | Command | What it does | When to run |
|--------|---------|-------------|-------------|
| Migrate | `{command}` | Runs pending migrations | After pulling changes with new migrations |
| Seed | `{command}` | Loads dev seed data | After fresh DB setup |
| Reset | `{command}` | Drops and recreates DB | When you need a clean slate |

## Deployment

| Script | Command | What it does | Who runs it |
|--------|---------|-------------|-------------|
| Build | `{command}` | {description} | CI / local |
| Deploy | `{command}` | {description} | {who/when} |
